<!DOCTYPE html>
<html lang="en">

<head>
    <title></title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <% include partials/links %>
</head>

<body>
    <% include partials/flashmessage %>
    <div class="mainContainer">
        <div class="content">
            <% include partials/sidebar %>
            <div class="innerContainer">
                <div class="heading">
                    <h3>Activity Details</h3>
                </div>
                <div>
                    <div class="card">
                        <img src="https://cdn.pixabay.com/photo/2017/03/02/09/26/question-mark-2110767_960_720.jpg"
                            alt="task image">
                        <div class="card-body">
                            <h5 class="card-title "><%= activity.name %></h5>
                            <p class="card-text" id="card-text"><%= activity.description %>.</p>
                            <p class="date"><%= activity.activityType %></p>
                            <% if ( activity.activityType.toLowerCase() == "quiz") { %>
                            <a href="/activity/quiz/<%= activity._id %>">View Instructions</a>
                            <% } else { %>
                            <a href="/activity/poll/<%= activity._id %>">View Instructions</a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <% include partials/scripts %>
</body>

</html>